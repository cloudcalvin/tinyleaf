language: c

cache:
  directories:
    - "~/.platformio"

jobs:
  include:
    - name: "Arduino UNO build"
      env: BOARD="arduino:avr:uno"
    - name: "NodeMCU E12 (8266) build"
      env: BOARD="esp8266:esp8266:nodemcu"


before_install:
  - "/sbin/start-stop-daemon --start --quiet --pidfile /tmp/custom_xvfb_1.pid --make-pidfile --background --exec /usr/bin/Xvfb -- :1 -ac -screen 0 1280x1024x16"
  - sleep 3
  - export DISPLAY=:1.0
  - wget http://downloads.arduino.cc/arduino-1.8.12-linux64.tar.xz
  - tar xf arduino-1.8.12-linux64.tar.xz
  - sudo mv arduino-1.8.12 /usr/local/share/arduino
  - sudo ln -s /usr/local/share/arduino/arduino /usr/local/bin/arduino


install:
  - ln -s $PWD /usr/local/share/arduino/libraries/GreenhouseCalc
  - arduino --pref "boardsmanager.additional.urls=http://arduino.esp8266.com/stable/package_esp8266com_index.json" --save-prefs
  - arduino --install-boards esp8266:esp8266 --save-prefs


script:
  - arduino -v --verify --board $BOARD $PWD/examples/Basic.ino


notifications:
  email:
    on_success: change
    on_failure: change